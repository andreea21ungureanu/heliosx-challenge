export const submitConsultationData = (answers: Record<string, string>) => {
  // Simulating sending data to an API
  console.log(
    "Submitting data for consultation: " + JSON.stringify(answers, null, 2)
  );
};
